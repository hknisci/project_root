apiVersion: v1
kind: Secret
metadata:
  name: {{ include "nodejs-express.fullname" . }}-secret
  labels:
    app: {{ include "nodejs-express.name" . }}
type: Opaque
data:
  slack-webhook-url: {{ .Values.slackWebhookUrl | b64enc | quote }}
