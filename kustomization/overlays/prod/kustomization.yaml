resources:
  - ../../base
  
configMapGenerator:
  - name: nodejs-express-mysql-configmap
    behavior: merge  # Bu satırı ekleyerek çakışmaları çözebilirsiniz
    literals:
      - NODE_ENV=production
      - DATABASE_HOST=prod-db-service
      - DATABASE_USER=prod-admin
      - DATABASE_PASSWORD=prod-password
